name: oketo-rasa-mdbot

# The build section defines how to build the images of your development environment
# More info: https://www.okteto.com/docs/reference/manifest/#build
build:

  # You can use the following env vars to refer to this image in your deploy commands:
  #  - OKTETO_BUILD_RASA_REGISTRY: image registry
  #  - OKTETO_BUILD_RASA_REPOSITORY: image repo
  #  - OKTETO_BUILD_RASA_IMAGE: image name
  #  - OKTETO_BUILD_RASA_TAG: image tag
  rasa:
    context: .
    dockerfile: Dockerfile
    image: rasa-server-okteto:latest

  # You can use the following env vars to refer to this image in your deploy commands:
  #  - OKTETO_BUILD_RASA-ACTIONS_REGISTRY: image registry
  #  - OKTETO_BUILD_RASA-ACTIONS_REPOSITORY: image repo
  #  - OKTETO_BUILD_RASA-ACTIONS_IMAGE: image name
  #  - OKTETO_BUILD_RASA-ACTIONS_TAG: image tag
  rasa-actions:
    context: .
    dockerfile: Dockerfile
    image: rasa-server-okteto:latest

# The deploy section defines how to deploy your development environment
# More info: https://www.okteto.com/docs/reference/manifest/#deploy
deploy:
  compose:
    file: docker-compose.yml
    services:
      - rasa
      - rasa-actions

# The dependencies section defines other git repositories to be deployed as part of your development environment
# More info: https://www.okteto.com/docs/reference/manifest/#dependencies
# dependencies:
#   - https://github.com/okteto/sample


# The dev section defines how to activate a development container
# More info: https://www.okteto.com/docs/reference/manifest/#dev
dev:
  rasa:
    image: rasa-server-okteto:latest
    command: bash
    workdir: /app
    sync:
      - .:/app
    forward:
      - 5005:5005
      - 5006:5005
    reverse:
      - 9000:9000
    volumes:
      - /root/.cache/pip
  rasa-actions:
    image: rasa-server-okteto:latest
    command: bash
    workdir: /app
    sync:
      - .:/app
    reverse:
      - 9000:9000
    volumes:
      - /root/.cache/pip

